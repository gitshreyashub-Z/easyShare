PORT=5000
MONGO_URI=your-mongodb-connection-string
JWT_SECRET=replace-with-a-long-random-secret
CLIENT_URL=https://easy-share-lemon.vercel.app
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-secret-key
SUPABASE_BUCKET=easyShare_uploads
# RECOMMENDED FOR RENDER & CLOUD HOSTS: Resend API (Uses HTTP port 443 - free 3,000 emails/month)
# NOTE: Render Free Tier BLOCKS outbound SMTP ports 25, 465, and 587.
# To send emails on Render, create a free key at https://resend.com and set RESEND_API_KEY:
RESEND_API_KEY=re_123456789
EMAIL_FROM="PasteBox <onboarding@resend.dev>"

# Alternative: SMTP Configuration (Only for hosts/local environments that do not block SMTP ports)
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_USER=your-email@gmail.com, SMTP_PASS=your-16-char-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password-or-app-password

